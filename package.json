{
  "author": "opensource@nativecode.com",
  "license": "MIT",
  "main": "index.js",
  "name": "native",
  "private": true,
  "version": "1.0.0",
  "devDependencies": {
    "@alrra/travis-scripts": "^3.0.1",
    "@nofrills/tasks": "^2.1.0",
    "coveralls": "^3.0.0",
    "lerna": "^3.3.0",
    "mocha": "^5.2.0",
    "nyc": "^13.0.1",
    "source-map-support": "^0.5.9",
    "ts-node": "^7.0.1",
    "tslint": "^5.11.0",
    "tslint-config-standard": "^8.0.1",
    "typescript": "~2.7.0"
  },
  "nyc": {
    "all": true,
    "exclude": [
      ".cache",
      "node_modules",
      "**/src/**/*.spec.ts",
      "**/src/Logger.ts"
    ],
    "extension": [
      ".ts"
    ],
    "include": [
      "packages/*/src/**/*.ts"
    ],
    "instrument": true,
    "report-dir": ".cache/coverage",
    "reporter": "text",
    "sourceMap": true,
    "temp-directory": ".cache/nyc"
  },
  "scripts": {
    "build": "cli-tasks build",
    "start": "cli-tasks start",
    "test": "cli-tasks test",
    "docker:build": "cli-tasks docker:build",
    "docker:push": "cli-tasks docker:push",
    "push": "sh push.sh",
    "travis-ssh": "cli-tasks configure",
    "do": "lerna"
  },
  "tasks": {
    "build": [
      "[clean]",
      "lerna run build"
    ],
    "configure": [
      "set-up-ssh --key \"$encrypted_c428ddd815a2_key\" --iv \"$encrypted_c428ddd815a2_iv\" --path-encrypted-key \"deploy.enc\""
    ],
    "clean": [
      "rimraf .cache"
    ],
    "docker:build": [
      "lerna run docker"
    ],
    "docker:push": [
      "lerna run docker-push"
    ],
    "start": [
      "lerna run start"
    ],
    "start:dev": [
      "lerna run start:dev"
    ],
    "test": [
      "[build]",
      "cross-env DEBUG= nyc mocha --opts mocha.opts"
    ]
  },
  "workspaces": {
    "nohoist": [
      "**/codelyzer",
      "**/codelyzer/*"
    ],
    "packages": [
      "interfaces/*",
      "packages/*",
      "services/*"
    ]
  }
}
